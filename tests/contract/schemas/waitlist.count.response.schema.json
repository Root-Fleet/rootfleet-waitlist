{
  "$id": "waitlist.count.response",
  "type": "object",
  "additionalProperties": false,
  "required": ["ok", "count", "rid"],
  "properties": {
    "ok": { "type": "boolean" },
    "count": { "type": "integer", "minimum": 0 },
    "rid": { "type": "string", "minLength": 1 }
  }
}
