{
  "$id": "waitlist.signup.response",
  "type": "object",
  "additionalProperties": false,
  "required": ["ok", "status", "message", "rid"],
  "properties": {
    "ok": { "type": "boolean" },
    "status": {
      "type": "string",
      "enum": ["joined", "already_joined"]
    },
    "message": { "type": "string", "minLength": 1 },
    "rid": { "type": "string", "minLength": 1 }
  }
}
